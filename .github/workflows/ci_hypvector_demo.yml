name: hypvector-demo
on:
  workflow_dispatch:
  push:
    branches: ["master"]
  pull_request:
    paths:
      - 'hypvector/**'
      - '.github/workflows/_common_jobs.yml'
      - '.github/workflows/ci_hypvector_demo.yml'
jobs:
  ci:
    uses: ./.github/workflows/_common_jobs.yml
    with:
      workspace: hypvector
