name: hypgrep-demo
on:
  workflow_dispatch:
  push:
    branches: ["master"]
  pull_request:
    paths:
      - 'hypgrep/**'
      - '.github/workflows/_common_jobs.yml'
      - '.github/workflows/ci_hypgrep_demo.yml'
jobs:
  ci:
    uses: ./.github/workflows/_common_jobs.yml
    with:
      workspace: hypgrep
