name: iceberg-hypgrep-demo
on:
  workflow_dispatch:
  push:
    branches: ["master"]
  pull_request:
    paths:
      - 'iceberg-hypgrep/**'
      - '.github/workflows/_common_jobs.yml'
      - '.github/workflows/ci_iceberg_hypgrep_demo.yml'
jobs:
  ci:
    uses: ./.github/workflows/_common_jobs.yml
    with:
      workspace: iceberg-hypgrep
